我上传一张图片, 我想用JavaScript代码在html页面上来实现这个功能,注意, JavaScript是个单独的文件, 让html去外链它. 

效果即: 有一个"总的长条时间轴", 上面以每100年为间隔 (间隔处用竖线表示). 然后上面用箭头指向对应的年份 (特定年份用小黑圆点表示), 有特定的历史事件说明. 

特定年份, 和该年份的历史内容文字说明, 我想写在一个单独的json文件里, 比如:

json_data = {
"-221": ["前221年,秦统一六国","red"], 
"-213": ["前213,212年,秦始皇焚书","black"],
"-207":["前207年,秦亡","red"],
"-202":["前202年,西汉建立","black"] 
}

这里面, 键值对中的key字符串部分, 即公元前的年份, 使用负数表示的. 公元后的年份, 就用正数表示.
键值对的value部分, 是一个数组, 里面第一个元素, 是该年份的历史事件说明. 第二个元素, 是表示字体颜色, 比如上面的例子, "red"就表示"前221年,秦统一六国"的文字用红色显示. "black"就表示"前202年,西汉建立"的文字用黑色显示.

然后JavaScript程序就读取这个json文件, 读取里面的每个key作为时间, 定位到"总的长条时间轴"上的对应位置. 该key对应的value值, 就是该年份的历史事件说明, 放到小黑原点上的箭头后方.




并且, 渲染后的html页面上, 还要有缩放调节功能, 即增加两个按钮, 一个能放大时间轴, 一个是能缩小时间轴, 以便当json中的键值对内容太多时, 能放大时间轴, 来看清每一个年份事件的细节.

另外, JavaScript代码还要有这些功能:
能调节文字的大小,
能调节小黑原点的大小,
能让用户继续增加100年的间隔, 比如我上传的图中, 间隔只标示到了400年, 让我能继续增加下去, 能标示到500年, 600年, ... 2000年等等


给出JavaScript和html页面代码, 并解释它是如何实现这些功能的